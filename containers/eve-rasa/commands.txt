-- Treina o modelo NLU
python -m rasa_nlu.train -c nlu_config.yml --data nlu.md -o models --fixed_model_name nlu --project current --verbose

-- Se quiser testar o intent, o arquivo test_model.py faz esse teste, no momento ta fixo lá no código a frase
python test_model.py

-- Treina o modelo do dialogo
python -m rasa_core.train -d domain.yml -s stories.md -o models/dialogue --endpoints endpoints.yml

-- Se quiser debugar o rasa core, tem que primeiro iniciar o server das actions
-- O primeiro comando instala o SDK que inicializa o server
-- O segundo comando inicializa o server e o terceiro comando roda o rasa no modo debug

pip install rasa_core_sdk
python -m rasa_core_sdk.endpoint --actions actions
python -m rasa_core.run -d models/dialogue -u models/current/nlu --debug --endpoints endpoints.yml